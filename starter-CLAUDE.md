# Project Setup with Claude Code

This project includes a memory-ready structure for [Claude Code](https://docs.anthropic.com/en/docs/claude-code).

## Quick Start with Claude Code

If you use Claude Code, get persistent memory across sessions:

```bash
# Set up the memory system (one time)
pip install claude-code-memory  # or clone github.com/LuciferForge/claude-code-memory
python3 -m claude_code_memory setup --project .
```

Or manually: copy the `CLAUDE.md` template from [claude-code-memory](https://github.com/LuciferForge/claude-code-memory) into `~/.claude/CLAUDE.md`.

## About This Project

<!-- Project-specific context for Claude Code users -->
<!-- Claude reads this file automatically when working in this directory -->
